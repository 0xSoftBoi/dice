interface:
  display_name: "Dice"
  short_description: "Repo-specific patterns and workflows for dice"
  default_prompt: "Use the dice repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true